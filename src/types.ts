export interface myProjectType {
  appLogo: string;
  appName: string;
  description: string;
  githubLink: string;
  liveSiteLink: string;
}

export interface osuProfileType {
  isFetching: boolean;
  data: unknown | string;
}
